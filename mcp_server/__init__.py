"""MCP server for the Xiaohongshu AI Agent project.

Exposes the project's capabilities (news fetching, content generation,
multi-agent pipeline, publisher, analytics) as MCP tools so any compliant
host (Claude Desktop, Cursor, Continue, custom clients) can drive it.
"""
